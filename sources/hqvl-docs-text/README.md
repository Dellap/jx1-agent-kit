# sources/hqvl-docs-text — tài liệu HQVL đã bóc text

Text thuần (bóc từ HTML) của 66 tài liệu cộng đồng **Hội Quán Võ Lâm**
(`github.com/jxoffline/{wiki-beta,jxtools,jx1-scripts,tutorials}`) để `grep` nhanh
mà không cần mở HTML. Index đầy đủ: `skills/jx1-hqvl-knowledge/references/doc-index.md`.

Đã xử lý khi mirror: cắt tham số `?jwt=...` (link ảnh ký hết hạn) và thay IP LAN của bên thứ ba
trong một bài viết bằng `<LAN_IP>`. Nội dung khác giữ nguyên.

Bản quyền: xem `NOTICE.md` — nội dung thuộc HQVL và tác giả từng bài, dùng cho nghiên cứu/phi thương mại.
